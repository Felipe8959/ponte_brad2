# Supondo que df_novo tenha os 30k + 9k
contagem = df_novo['TARGET_FULL'].value_counts()

print(f"Total de Categorias: {len(contagem)}")
print(f"Categorias com menos de 10 exemplos: {len(contagem[contagem < 10])}")

# DICA DE OURO:
# Se você tiver muitas categorias com < 10 exemplos, o modelo NÃO vai aprender elas.
# Elas vão apenas gerar ruído e confundir as categorias maiores.
